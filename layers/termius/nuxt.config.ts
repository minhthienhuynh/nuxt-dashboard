// Termius feature layer: SSH manager UI (and, via the web-terminal change, the
// terminal view). Extended from the root nuxt.config.ts. Kept minimal here —
// the web-terminal change adds `nitro.experimental.websocket` when applied.
export default defineNuxtConfig({})
